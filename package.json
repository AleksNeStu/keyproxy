{
  "name": "keyproxy",
  "version": "1.0.0",
  "main": "main.js",
  "scripts": {
    "start": "node main.js",
    "dev": "node index.js",
    "test": "node --test test/**/*.test.js",
    "test:coverage": "node --experimental-test-coverage --test test/**/*.test.js",
    "test:watch": "node --test --watch test/**/*.test.js"
  },
  "keywords": [
    "api-key",
    "keyproxy",
    "mcp",
    "mcp-server",
    "key-rotation",
    "proxy",
    "api-key-management",
    "claude-desktop",
    "cursor",
    "windsurf",
    "brave-search",
    "exa",
    "jina",
    "firecrawl"
  ],
  "author": "Alex Nesterovich",
  "license": "MIT",
  "description": "Local API key vault + proxy for MCP servers. Keep real keys out of config files — intercept, rotate, and health-check transparently.",
  "dependencies": {
    "joi": "^18.1.2",
    "node-windows": "^1.0.0-beta.8"
  }
}
